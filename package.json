{
  "name": "ingestio",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source background document operations platform: multi-tenant PDF uploads, AI extraction on BullMQ/Upstash Redis, metadata in Supabase Postgres, webhook notifications.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ingestio/web",
    "dev:worker": "npm run dev --workspace @ingestio/worker",
    "start:worker": "npm run start --workspace @ingestio/worker",
    "build": "npm run build --workspace @ingestio/web && npm run build --workspace @ingestio/worker",
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "supabase db push"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
